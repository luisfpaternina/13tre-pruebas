<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_helpdesk_tool_brand_search" model="ir.ui.view">
        <field name="name">view.helpdesk_tool_brand.search</field>
        <field name="model">helpdesk.tool.brand</field>
        <field name="arch" type="xml">
            <search string="Helpdesk Tool Brand Search">
                <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
                <separator/>
                <field name="name" filter_domain="[('name', 'ilike', self)]"/>
            </search>
        </field>
    </record>

    <record id="view_helpdesk_tool_brand_form" model="ir.ui.view">
        <field name="name">view.helpdesk_tool_brand.form</field>
        <field name="model">helpdesk.tool.brand</field>
        <field name="arch" type="xml">
            <form string="Helpdesk Tool Brand">
                <header>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1><field name="name"/></h1>
                    </div>
                    <group name="main">
                        <field name="year_ids" widget="many2many_tags"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_helpdesk_tool_brand_tree" model="ir.ui.view">
        <field name="name">view.helpdesk_tool_brand.tree</field>
        <field name="model">helpdesk.tool.brand</field>
        <field name="arch" type="xml">
            <tree string="Helpdesk Tool Brands">
                <field name="name"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <record id="action_helpdesk_tool_brand" model="ir.actions.act_window">
        <field name="name">Tool brands</field>
        <field name="res_model">helpdesk.tool.brand</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_helpdesk_tool_brand_search"/>
        <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new tool brand
                </p>
        </field>
    </record>

    <menuitem id="menu_helpdesk_tool_brand" action="action_helpdesk_tool_brand"
        sequence="10" parent="helpdesk.helpdesk_menu_config"/>

</odoo>


