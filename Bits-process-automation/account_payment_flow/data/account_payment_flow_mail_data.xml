<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="new_template_payment_flow_template" model="mail.template">
            <field name="name">Template: Prepare Payment</field>
            <field name="model_id" ref="account_payment_flow.model_account_payment_flow"/>
            <field name="subject">${object.display_name}</field>
            <field name="email_from">${(object.write_uid.email_formatted or user.email_formatted) | safe}</field>
            <field name="email_to">${(object.partner_id.email_formatted if not object.sudo().partner_id.email or object.sudo().partner_id.email != object.partner_id.email_formatted else '') | safe}</field>
            <field name="partner_to">${object.partner_id.id if object.sudo().partner_id.email and object.sudo().partner_id.email == object.partner_id.email_formatted else ''}</field>
            <field name="body_html" type="xml">
                <div>
                    Dear ${object.sudo().partner_id.name or 'Madam/Sir'},<br /><br />
                    The payment is prepared.<br />
                    Continue the process.<br /><br />
                    Thank you,<br/><br/>
                    ${object.stage_id.name or 'Payment Flow Stage'} Team.
                </div>
            </field>
            <field name="lang">${object.partner_id.lang or object.user_id.lang or user.lang}</field>
            <field name="auto_delete" eval="False"/>
        </record>
    </data>
</odoo>
