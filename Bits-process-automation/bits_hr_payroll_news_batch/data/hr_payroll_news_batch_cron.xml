<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_create_payroll_new_batch" model="ir.cron">
            <field name="name">Create payroll new batch</field>
            <field name="model_id" ref="model_hr_payroll_news_batch" />
            <field name="state">code</field>
            <field name="type">ir.actions.server</field>
            <field name="code">model._generate_fixed_novelties()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="nextcall" eval="(DateTime.now().replace(day=1) + relativedelta(months=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="numbercall">-1</field>
            <field name="active">1</field>
        </record>
    </data>
</odoo>